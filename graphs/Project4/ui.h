#ifndef UI_H
#define UI_H

#include <bits/stdc++.h>
#include "solver.h"

class ui {
    public:
        ui();
        void start();
        void schedule();
        void distinctPaths();
        void distMin();
        void buildTree();
};

#endif // UI_H
