#ifndef TIMELINE_H
#define TIMELINE_H

#include "battle.h"
#include <bits/stdc++.h>
/*
class timeline {
    private:
        std::vector<battle> l;

    public:
        timeline();
        int getTimes() {return l.size();}
        void add(battle);
        void del();
        battle getCurrent();
};
*/
#endif // TIMELINE_H
